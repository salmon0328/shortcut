# src/

See the top-level [README](../README.md) for what this project is and how to
run it.

`shortcut/` is the routing service. `shortcut/ai/` is the agentic layer and
has [its own README](shortcut/ai/README.md).
